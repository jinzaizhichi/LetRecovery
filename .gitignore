# Rust 构建产物
/target
**/target/

# Cargo.lock 由 CI 重新生成，不纳入版本控制
Cargo.lock

# 临时 / 日志文件
nlog.txt
*.tmp
*_tmp.json
body_tmp.json
resp_tmp.json

# Ghost 运行时输出
*.dmp
GHOSTERR.TXT
ghstwarn.txt

# 编辑器 / 系统垃圾
.DS_Store
Thumbs.db
*.swp
新建文本文档.bat
